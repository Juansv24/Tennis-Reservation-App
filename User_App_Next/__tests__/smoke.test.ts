// ABOUTME: Smoke test to verify the Jest + Testing Library setup works.
// ABOUTME: Can be deleted once real tests exist.
describe('Jest setup', () => {
  it('runs a basic test', () => {
    expect(1 + 1).toBe(2)
  })
})
